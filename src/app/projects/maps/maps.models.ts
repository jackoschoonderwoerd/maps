export interface Bridge {
    name: string,
    lat: number,
    lon: number
}
export interface DistanceToBridge {
    name: string;
    meters: number;
}

export interface VisitorLocation {
    lat: number;
    lon: number;
}

